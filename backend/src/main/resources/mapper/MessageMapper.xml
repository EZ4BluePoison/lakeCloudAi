<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lakecloud.ai.mapper.MessageMapper">

    <resultMap id="BaseResultMap" type="com.lakecloud.ai.entity.Message">
        <id column="id" property="id"/>
        <result column="conversation_id" property="conversationId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, conversation_id, role, content, created_at
    </sql>

    <select id="findByConversationId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM messages WHERE conversation_id = #{conversationId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO messages (conversation_id, role, content, created_at)
        VALUES (#{conversationId}, #{role}, #{content}, NOW())
    </insert>

    <delete id="deleteByConversationId">
        DELETE FROM messages WHERE conversation_id = #{conversationId}
    </delete>

</mapper>
