<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lakecloud.ai.mapper.KnowledgeMapper">

    <resultMap id="BaseResultMap" type="com.lakecloud.ai.entity.Knowledge">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="file_url" property="fileUrl"/>
        <result column="file_type" property="fileType"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, title, content, file_url, file_type, created_at
    </sql>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM knowledge_base WHERE user_id = #{userId}
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/> FROM knowledge_base WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO knowledge_base (user_id, title, content, file_url, file_type, created_at)
        VALUES (#{userId}, #{title}, #{content}, #{fileUrl}, #{fileType}, NOW())
    </insert>

    <delete id="deleteById">
        DELETE FROM knowledge_base WHERE id = #{id}
    </delete>

</mapper>
